<div class="card">
  <div class="card-body">
    <h5 class="card-title">Upload video for attendance</h5>
    <form action="/upload" method="POST" enctype="multipart/form-data">
      <div class="mb-3">
        <label class="form-label">Video file (mp4/avi)</label>
        <input class="form-control" type="file" name="video" accept="video/*" required />
      </div>

      <div class="row">
        <div class="col-md-4">
          <label class="form-label">Frames/sec to sample</label>
          <input class="form-control" type="number" name="sample_fps" min="0.1" step="0.1" value="1.0" />
          <div class="form-text">Higher = slower, but more accurate</div>
        </div>
        <div class="col-md-4">
          <label class="form-label">Min matching frames</label>
          <input class="form-control" type="number" name="min_frames" min="1" step="1" value="1" />
          <div class="form-text">Frames required to consider a person present</div>
        </div>
      </div>

      <div class="mt-3">
        <button class="btn btn-primary" type="submit">Upload & Process</button>
      </div>
    </form>
  </div>
</div>